# 2026-08-27

## 输入系统迁移（续）：用户询问 PlayerInput 方案
- 现状确认：已装 com.unity.inputsystem 1.14.2，ProjectSettings activeInputHandler=2（Both 共存），PlayerObject.cs 已加 using UnityEngine.InputSystem 但 Update() 仍是旧 API。
- 用户关注点：新输入 Keyboard.isPressed 是 0/1 阶跃，直接 SetFloat 会跳过走路动画；已给出 Mathf.MoveTowards(_v/_h, target, 3f*deltaTime) 复现旧 GetAxis（Input Manager 默认 Gravity/Sensitivity=3）的方案。
- 游戏设计澄清：Shift 键只切瞄准层 animator.SetLayerWeight(1, 1/0)，跑步与否由 VSpeed/HSpeed 数值决定（用户强调）。
- 用户倾向 PlayerInput + .inputactions 正规方案；已推荐 .inputactions 生成 C# 类方案（不挂组件也可用），并说明平滑仍需自处理。用户习惯：给方案代码自己粘贴，不代改。

## 调试过程记录（同日）
- PlayerObject.cs 曾报 NullReferenceException（L49）：原因是只贴了 Update()，漏了 Awake() 里 `controls = new PlayerControls()` + OnEnable/OnDisable 的 Enable/Disable，已指导补全。
- PlayerControls.inputactions 的 Action Map 名为 "Player"（非 Gameplay），Shift 键 action 名为 "Squat"（带 Hold interaction，IsPressed 不受影响）。PlayerObject/GamePanel 均已改造完成；GamePanel 用的是 Keyboard.current 直读方案（digit1/2/3Key、spaceKey）。
- 鼠标旋转过快问题：Look 绑定 <Pointer>/delta 是原始像素，旧版 GetAxis 经 Input Manager Mouse X Sensitivity(默认0.1) 缩放，roundSpeed=50 是按缩小后值配的 → 建议加 mouseSensitivity=0.1f 系数复现旧手感。
- 当前 activeInputHandler=2(Both)：ArtRes 8 个第三方脚本仍用旧 API，切 1 前必须先处理。
