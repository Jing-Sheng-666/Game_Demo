# 2026-09-02 工作记录

## 相机视角改动讨论
- 用户想把当前俯视角改为 TPS 肩射/腰射视角（参考截图，低跟随第三人称视角）
- 关键问题：`CameraMove` 的 LookAt 点指向角色头顶偏上，导致相机视线 ≠ 角色 forward，射击靠猜
- 第一步方案：改 `CameraMove.Update` 中 `LookRotation` 的目标点，从"看角色头顶"改为"看角色正前方远处"，这样屏幕中心即命中方向
- 给出一组推荐初值：offsetPos (0.5, 1.5, -3.0)，lookForwardDist 取 5~10，lookHeightOffset 取 1.4
- 用户表示先改视角再谈 ADS/腰射切换
- 注意事项：当前 `PlayerObject` 的 Look 只有 yaw（水平）无 pitch（俯仰），改视角后只能左右瞄准不能上下，需要在第二阶段一并修复

## 进展（11:25）
- 用户已按第一步方案调好相机视角（TPS 跟随后，offsetPos/lookForwardDist/lookHeightOffset 已调参）
- 第二阶段方案已给出：加 pitch 俯仰
  - PlayerObject 累计 pitch（clamp ±60°），通过 `cam.SetPitch(pitch)` 传给 CameraMove
  - CameraMove 用 `aimDir = Quaternion.AngleAxis(pitchDeg, target.right) * target.forward` 计算 aimPoint
  - ShootEvent 射线方向同步改为 aimDir，保证屏幕中心=子弹方向
  - 提醒：方向符号可能需取负，灵敏度可单独调
- 下一步计划：右键 ADS 切换（相机拉近）+ 准星 UI

## 角色是否随视角动作（11:29）
- 用户问：改视角后角色会不会跟着动。结论：
  - 左右转（yaw）：会动——`transform.Rotate(Vector3.up)` 转的是玩家整个物体，模型是子物体跟着转（现状已如此）
  - 抬头/低头（pitch）：按方案只有相机动，玩家物体不旋转 → 角色保持直立，这是标准 TPS 做法（PUBG/古墓丽影），且子弹方向已用 pitch 修正，不影响打准
  - 若想要角色跟枪：转骨骼 `animator.GetBoneTransform(Spine/Chest/Head).localRotation = Quaternion.Euler(pitch*系数,0,0)`（注意与动画打架）/ Aim 动画层 / Animation Rigging IK

## 第三阶段：ADS + 准星（14:34）
- 用户已修好 pitch 方向（look.y 取负累加），放弃角色跟枪动画（方案一不行）
- 给出 ADS + 准星完整方案（用户自己改代码，权限只读）：
  - 核心原则：ShootEvent 射线改从相机发（origin = cam.pos + cam.forward*0.5，dir = cam.forward），保证屏幕中心=准星=命中点，与枪口/角色朝向/pitch 全部解耦，ADS 过渡中也成立；之前 aimDir pitch 修正可删
  - inputactions 加 Aim（Button + rightButton + Hold）自动重新生成 PlayerControls.cs
  - CameraMove：加 adsOffset/adsMoveSpeed，Awake 记录 hipOffset=offsetPos，SetAim(bool)，Update 用 1-Exp(-8dt) 帧率无关插值切机位
  - PlayerObject：Aim.IsPressed() → cam.SetAim + crosshairUI.SetActive；可选 ADS 灵敏度 ×0.6
  - 顺带修 bug：RaycastAll 结果按 distance 排序再取最近怪（原代码顺序不定）
  - 准星 UI：Canvas 中心 4 条 Image 拼十字，拖到 PlayerObject.crosshairUI，仅 ADS 显示
- 已知边界：射线 mask 仅 Monster 层，会穿墙；以后加掩体需改 mask 取最近 hit
- 下一步：用户改完验证后，可能做腰射散布/连发等

## 追加调整（15:01）
- 准星改为常显（腰射也要瞄准，射线从相机发所以腰射也准），ADS 时缩放（crosshair.localScale Lerp 到 0.5，字段改 RectTransform）
- 鼠标：进游戏场景 Locked + visible=false（GamePanel.Init 原 Confined 处改）；ESC 开 QuitPanel 时解锁显示，关面板恢复 Locked；切回主菜单也要解锁 + timeScale=1
- 建议封装 GamePanel.LockCursor()/UnlockCursor() 静态方法复用
- 注意：Locked 模式下 UI 点不到，面板必须先解锁再显示；窗口失焦后 Locked 不自动恢复

## 代码现状核对 + 精确改动清单（15:20）
- 读了 5 个文件，确认大部分已改好：CameraMove（ADS/SetAim/插值/pitch）✓、inputactions 的 Aim（rightButton+Hold）✓、QuitPanel ESC 逻辑 ✓、PlayerObject 的 pitch/cam/crosshair 字段 ✓
- 仍缺 5 处代码 + 1 处场景：
  1. PlayerObject.Update 第 94 行注释上方插：Aim.IsPressed() → cam.SetAim + crosshair.localScale Lerp（aiming?0.5:1）
  2. PlayerObject.ShootEvent 131-132 行：gunPoint+aimDir 改为 cam.pos+cam.forward*0.5 + cam.forward，并加 RaycastAll 按 distance 排序
  3. GamePanel.Init 第 37 行：Confined → Locked + visible=false
  4. QuitPanel.ShowMe 52 行：补 Cursor.visible=true
  5. QuitPanel.ContinueGame 40 行：Confined → Locked + visible=false；btnBack(20 行)加 None+visible=true（切主菜单）
  6. 场景：Canvas 中心 4 条 Image 拼十字（2×12/12×2，±10 偏移，去 Raycast Target）命名 Crosshair，拖 PlayerObject.crosshair
- 注意：用户把 pitch clamp 改成了 ±10（抬头幅度小），建议改回 ±60

# 2026-09-02
- 设计了 ESC 退出面板方案（未写代码，仅讨论）：QuitPanel 继承 BasePanel；ESC 检测放 GamePanel.Update（面板隐藏时会被 UIManager 销毁，无法自监听）；用 Time.timeScale=0 暂停，需把 BasePanel 淡入淡出改用 Time.unscaledDeltaTime，切回主菜单前必须恢复 timeScale=1（跨场景不重置）。用户计划移除 GamePanel 的 btnQuit，退出逻辑迁移到 QuitPanel。

## 准星改为纯代码动态创建（15:45）
- 用户 Canvas 是 UIManager 私有构造里 Instantiate("UI/Canvas") + DontDestroyOnLoad（开始场景创建、跨场景常驻），GamePanel 进游戏场景时才 ShowPanel 挂上去 → Inspector 摆准星不可行
- 方案：PlayerObject 内 CreateCrosshair() 纯代码建（FindObjectOfType<Canvas>() 找常驻 Canvas，根节点锚 0.5/0.5 居中，4 根 Image 条 2×12/12×2 偏移 ±10，Image 无 sprite 即纯色矩形、raycastTarget=false），Awake 里 crosshair = CreateCrosshair()
- 关键坑：Canvas 是 DontDestroyOnLoad 的，准星挂它下面回主菜单不消失 → 必须 PlayerObject.OnDestroy() 里 Destroy(crosshair.gameObject)
- 字段从 [SerializeField] RectTransform crosshair 改成私有代码赋值，不再 Inspector 拖
- pitch clamp 仍提醒用户在 ±10，建议改回 ±60
- 15:49 修正：用户已自建 Crosshair 预设体，放弃纯代码创建方案 → 改为 Addressables 地址 UI/Crosshair + Instantiate(FindObjectOfType<Canvas>().transform, false) + OnDestroy 清理（Canvas 常驻这点不变）
- 15:53 检查结果：PlayerObject（准星加载/OnDestroy/相机射线+排序/ADS 缩放）✓、CameraMove（ADS 双机位插值/pitch）✓、inputactions Aim=rightButton+Hold ✓、QuitPanel（ESC 解锁/继续锁回/切主菜单解锁+timeScale）✓、GamePanel.Init Locked ✓
- 遗留提醒：pitch clamp 仍在 ±10（抬头低头幅度小，建议 ±60）；直接开 GameScene 测试时 Canvas 可能未创建，FindObjectOfType<Canvas>() 会 NRE（可先触发 UIManager.Instance 再找）

## GameOverPanel bug + ADS 调参（15:57）
- Bug：GameOverPanel.ShowMe() 只设 Cursor.lockState=None，漏了 Cursor.visible=true（GamePanel.Init 设过 false）→ 结束面板弹出鼠标隐形。修复：ShowMe 补 Cursor.visible=true
- ADS 调参指引：adsOffset x/y/z=右偏/高度/离肩距离（推荐起点 (0.75,1.55,-1.5)）；lookForwardDist 8~15 + lookHeightOffset 与相机 y 配平；rotationSpeed 低会导致准星追不上鼠标；可选加 FOV 收窄（ADS 60/腰射 75，CameraMove.Update 里 lerp fieldOfView）
- 用户感觉"肩射视角怪"，怀疑缺 FOV 变焦效果

## 场景 2/3 相机参数同步（16:10）
- 调查发现：GameScene2/3 的 CameraMove 还是旧俯视参数（offsetPos (0,8,-9)、bodyHeight 2，无新字段），GameScene1 已调好 (2,3,-4)/lookForwardDist 5/lookHeightOffset 1.4/moveSpeed 10/rotationSpeed 20/adsOffset (1,2,-2)/adsMoveSpeed 12
- 用户授权直接改场景 2/3：用 Edit 把两场景 CameraMove 组件字段块整体替换为场景 1 同款（脚本 GUID 1415c5a26ecc7aa4c89709251df1acf7，组件/物体 fileID 三场景一致 1457894435/1457894428），删除旧 bodyHeight 字段，已验证三场景块一致
- 角色数据调查：RoleInfo 共 6 个（ResLocal/RoleData/RoleInfo*.asset，Addressables label RoleData）；type 1=近战（Role/1 小刀兵 weapon_knife，免费默认）、type 2=远程（2 手枪/3 狙击/4 步枪/5 机枪/6 火箭筒，weapon_handgun 等）；KnifeEvent 走 OverlapSphere 近战判定与相机射线无关 → 刀兵不需要准星/ADS 体系，用户决定"刀兵不管"（后续可做可不做 type 分支）

## 异步加载审查（16:40，纯只读分析，未动代码）
- 根因：业务层大量使用 `AddressablesMgr.LoadAssetSync`（AddressablesMgr.cs:224-238，内部 `LoadAssetAsync`+`WaitForCompletion` 主线程忙等）；远程 bundle 场景下卡顿=下载耗时
- P0 启动阻塞：GameDataMgr 构造（GameDataMgr.cs:11 static new；35-58 内 2×JsonMgr 同步 IO + 4×`LoadAssetsAsync(...).WaitForCompletion()`，首次触发 BKMusic.Awake:20）；UIManager 构造（UIManager.cs:21 LoadAssetSync UI/Canvas，Main.Start:17 触发）
- P0 战斗热路径：TowerObject.Update:70/87 每次开火同步加载特效；GameDataMgr.PlaySound:84 每次音效同步加载 AudioClip（8 处调用：开火/受伤/死亡/啃塔/刀/枪）；MonsterPoint.CreateMonster:66 + MonsterObject.InitInfo:35 每只怪同步加载 prefab+animator；PlayerObject.ShootEvent:175 每命中同步加载 hitEff
- P1 交互点击：UIManager.ShowPanel:38（所有面板首开）；ChooseHeroPanel.ChangeHero:131（切英雄）；ChooseScenePanel.ChangeScene:73（场景图）；TowerBtn.InitInfo:38；TowerPoint.CreateTower:45；GameLevelMgr.InitInfo:42（玩家）；PlayerObject.Awake:41（Crosshair）
- P2：GameOverPanel:26/QuitPanel:29 同步 LoadScene 回主菜单；JsonMgr 写盘低频
- 已有异步基建（复用即可）：AddressablesMgr.LoadAssetAsync/LoadAssetsAsync（带缓存+引用计数）；UpdateMgr 热更协程；ChooseScenePanel:46 已用 LoadSceneAsync
- 顺带发现：引用计数泄漏——所有 LoadAssetSync/缓存命中都 count+1 但调用方 Destroy 后从不 ReleaseAsset（特效/英雄预览/塔/面板），内存持续上涨
- 推荐方向：配置/Canvas 放启动或热更 loading 预热；塔特效/音效在波次开始前预加载并持引用；使用点改 LoadAssetAsync+回调

## 引用计数泄漏修复方案（17:15，只读分析，未改代码）
- 确认：全项目对 ReleaseAsset/ReleaseAssets/ClearAsset 零调用（仅 AddressablesMgr.cs 有定义）→ 计数只增不减
- +1 点（AddressablesMgr.cs）：L15 构造 count+=1、L50 LoadAssetAsync 命中、L155 LoadAssetsAsync 命中、L229 LoadAssetSync 命中；-1 在 L110/208 但无调用者 → count 永不归零，资源永不卸载 + AddressablesDic 无限膨胀
- 高频泄漏点：TowerObject:70/87 每炮特效、GameDataMgr:84 PlaySound 每音效、MonsterPoint:66+MonsterObject:35 每怪、PlayerObject:175 每命中、UIManager:21/38 面板、ChooseHeroPanel:131、ChooseScenePanel:73、TowerBtn:38（UpdateSelTower 反复触发）、TowerPoint:45、GameLevelMgr:42+PlayerObject:41
- 推荐修复 A（治本，业务零改动）：改计数模型为"驻留缓存"——L15 改 count=1；L50/155/229 命中分支删 count+=1；ReleaseAsset 语义变为显式卸载（count 1→0 即 Release+Remove）；代价=资源常驻（项目总量小可接受）。ClearAsset(L123) 慎用：会连 DontDestroyOnLoad 的 Canvas 一起释放，需常驻 key 白名单或不用
- 修复 B（严格引用计数）：逐调用点配对 Release（主菜单预览角色与局内玩家共用角色 res，两边都要配对，易漏）
- 顺带隐患：LoadAssetSync L224-238 若 ContainsKey 但 !IsDone（async 进行中 sync 同 key）会重复 Add → ArgumentException；改异步前必须把该分支改为 Convert+WaitForCompletion 已有 handle
