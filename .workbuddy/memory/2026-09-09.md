# 2026-09-09

## EventCenter 事件驱动改造（进行中）
- 用户导入 EventCenter.cs 报错：EventCenter 类内重复定义了两次私有构造函数（第70/76行），删除第76行空构造函数即可；另提示单例内冗余 null 检查可清理。
- 已确定改造方案：把塔防 Demo 由"God 类直调"改为事件驱动解耦。
- 现状耦合点：MonsterObject 直调 player.AddMoney/UIManager/Sound；MainTowerObject 直调 GamePanel；GameLevelMgr 直调 GamePanel 更新波数；GameOverPanel 由 Monster/MainTower 直接弹出。
- 设计方案（分步改，用户逐个文件手改，助手不改文件）：
  ① E_EventType.cs 枚举按领域扩：E_Monster_Dead(MonsterObject)/E_Player_MoneyChanged(int)/E_Level_WaveChanged(WaveArgs)/E_MainTower_HpChanged(MainTowerHpArgs)/E_Game_Over(GameOverArgs)；E_Player_GetReward、E_Test 全工程无引用可删。
  ② 新建 EventCenterArgs.cs：WaveArgs、MainTowerHpArgs、GameOverArgs 参数类。
  ③ PlayerObject：删 UpdateMoney()，AddMoney/InitPlayerInfo 改广播 E_Player_MoneyChanged。
  ④ GameLevelMgr：构造函数订阅 E_Monster_Dead 发击杀奖励(10)；UpdatgeMaxNum/ChangeNowWaveNum 改广播 WaveArgs；新增 GameWin() 广播胜利。注意若全局 Clear() 需改可重注册结构。
  ⑤ MonsterObject：Dead() 只广播 E_Monster_Dead，不再直调 player/UI；DeadEvent() 胜利时调 GameLevelMgr.GameWin()（保留 RemoveMonster/CheckOver/回池）。
  ⑥ MainTowerObject：UpdateHp/Wound 改广播；失败广播 E_Game_Over(半额金钱,false)。
  ⑦ GamePanel：OnEnable/OnDisable 订阅4类UI事件，OnGameOver 弹结算面板；旧公共更新方法保留。
  ⑧ 可选：SoundMgr 订阅 E_Monster_Dead 播死亡音效（懒加载时序风险，建议跑通主线后再做）。
- 音效采用"服务门面直调"(GameDataMgr.PlaySound) 不强行走事件。
- 用户偏好：改代码前先说明思路、给完整可粘贴代码、不要直接改他文件；当前处于逐个文件照抄阶段（已完成①）。
