# 2026-09-04 工作记录

## GameDataMgr InitAsync 改造确认 + 剩余异步点盘点（09:32，只读核对）
- ✅ 已确认落地：GameDataMgr InitAsync/InitSync/isInited/OnInitFinished（构造瘦身）；BKMusic 事件驱动（:19-29）；GameLevelMgr.InitInfo 兜底 InitSync（:34-35）；UpdatePanel.SwitchAfter yield InitAsync（:58）；SoundMgr clipDic（:34,101-109）
- ❌ 仍直接 LoadAssetSync（未处理）：
  1. MonsterObject.cs:47 animator —— 每次出池加载 + 无释放（唯一"值得单独改成缓存字典"的使用点）
  2. GameLevelMgr.cs:48 玩家 / PlayerObject.cs:41 Crosshair / TowerPoint.cs:45 塔 —— 每局/每建塔同步加载且不释放（未走池）
  3. UI 组：UIManager:21/38、ChooseHeroPanel:131、ChooseScenePanel:73、TowerBtn:38 —— 主菜单低频，首开卡 + 每次 +1 泄漏
- 判断：到现阶段"逐点改 async"边际收益低，更优是两步收口：
  ① 修复 A：AddressablesMgr 命中缓存删 count+=1（L50/155/229）+ LoadAssetSync 竞态合并（ContainsKey 即 Convert+WaitForCompletion，不再二次 Add）→ 一次性消除全部残留计数泄漏 + 为预热开路（注意：需同步核对 PoolMgr.ClearPool/SoundMgr 依赖 count 归零卸载的路径是否仍正常）
  ② 预热：进 GameScene 前把本关 塔/怪/特效/animator/音效 LoadAssetAsync + PoolMgr 预填充 → 玩家/塔/Crosshair/UI 即使保留 LoadAssetSync 也只是"早已缓存命中"
- 待用户选择下一步：单独做 MonsterObject animator 缓存 / 做修复 A 收口 / 做预热体系
