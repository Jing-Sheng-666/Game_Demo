using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FindData : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //打印persistentDataPath
        print(Application.persistentDataPath);
    }
}
